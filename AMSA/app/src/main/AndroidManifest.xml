<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ss174h.amsa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".startup.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <service android:name=".APKScanner.APKScannerService" />

        <activity android:name=".ViewAppsActivity" />
        <activity android:name=".APKScanner.CheckAppCertActivity" />
        <activity android:name=".CheckPermissions.ViewAppsPermsActivity" />
        <activity android:name=".CheckPermissions.CheckAppPermsActivity" />
        <activity android:name=".CheckPermissions.CheckAppPermissionsActivity"></activity>
    </application>

</manifest>